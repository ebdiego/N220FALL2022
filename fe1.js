//Eberto Diego
// N220
// 09/15/2022

let car = {
x: 200, y: 50
}

//Object created

function setup() {
    createCanvas(720, 480);

}

function drawCar() {
    circle(car.x, car.y, mouseX, mouseY);
}


let randColor = {
    color (170, 200, 150);
}

// Color of choice

function removeRed() {
     randColor.setRed(0); 

    return noRed;
}

// remove red from color

function polarPoint() {

    var x =  r * Math.sin(mouseX);
    var y = r * Math.cos(mouseX);

// draw coordinates

    return createVector(x,y); 


}
